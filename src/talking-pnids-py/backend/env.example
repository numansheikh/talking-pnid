# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-5.1
REASONING_EFFORT=medium

# Directory Paths (use absolute paths for cloud deployment)
# For local development, these can be relative to project root
# For cloud deployment, use absolute paths or full URLs
PDFS_DIR=./data/pdfs
JSONS_DIR=./data/jsons
MDS_DIR=./data/mds

# Optional: Set PROJECT_ROOT if paths should be relative to a specific directory
# PROJECT_ROOT=/path/to/project/root

# API Settings
MAX_TOKENS=2000
TEMPERATURE=0.7

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
